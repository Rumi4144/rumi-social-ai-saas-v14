import Link from "next/link";import {tenantContext} from "@/lib/auth/context";import {prisma} from "@/lib/prisma";
export default async function Dashboard(){const ctx=await tenantContext();const [campaigns,scheduled,published,sub,approval]=await Promise.all([prisma.campaign.count({where:{brand:{organizationId:ctx.organizationId}}}),prisma.publishJob.count({where:{organizationId:ctx.organizationId,status:"scheduled"}}),prisma.publishJob.count({where:{organizationId:ctx.organizationId,status:"published"}}),prisma.subscription.findUnique({where:{organizationId:ctx.organizationId}}),prisma.approvalShare.count({where:{organizationId:ctx.organizationId,status:"pending"}})]);return <><section className="hero v2"><div><div className="eyebrow">{ctx.organization.name.toUpperCase()} · CREATIVE OS</div><h1>What should we create today?</h1><p>Turn a product, service or idea into a complete campaign—then approve what goes live.</p><Link className="button gold" href="/create">✦ Create Everything</Link>{campaigns===0&&<Link className="ghost" href="/get-started">Finish guided setup</Link>}</div><div className="orb">AI<small>READY</small></div></section><div className="grid">{[["CAMPAIGNS",campaigns],["SCHEDULED",scheduled],["PUBLISHED",published],["AI CREDITS",sub?.credits||0]].map(x=><div className="card" key={String(x[0])}><span className="eyebrow">{x[0]}</span><div className="metric">{x[1]}</div></div>)}</div>{campaigns===0?<section className="emptylaunch"><span className="eyebrow">YOUR FIRST WIN</span><h2>Create your first campaign in minutes.</h2><p>Complete Brand Brain setup, give us one offer, and review the campaign before anything is scheduled.</p><Link className="button" href="/get-started">Start guided setup →</Link></section>:<div className="twocol"><section><h2>Workspace activity</h2><div className="pipeline"><div><b>{campaigns}</b><span>Campaigns</span></div><div><b>{approval}</b><span>Awaiting approval</span></div><div><b>{scheduled}</b><span>Scheduled</span></div><div><b>{published}</b><span>Published</span></div></div></section><aside className="nextbest"><span>AI WORKFLOW</span><h3>Keep approvals ahead of the publishing queue.</h3><p>You stay in control: AI prepares; your team approves.</p><Link className="button" href="/publishing">Open Publishing Center</Link></aside></div>}</>}