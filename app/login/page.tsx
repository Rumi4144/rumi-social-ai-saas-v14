"use client";import {useState} from "react";import {signIn} from "next-auth/react";
export default function Login(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[msg,setMsg]=useState("");async function go(e:any){e.preventDefault();setMsg("Signing in…");const x=await signIn("credentials",{email,password,redirect:false});if(x?.error)setMsg("Email or password is incorrect.");else location.href="/dashboard"}return <main className="loginwrap"><form className="loginbox" onSubmit={go}><div className="eyebrow">RUMI SOCIAL AI</div><h1>Welcome back.</h1><p>Sign in to your Creative OS.</p><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8}/><button className="button">Sign in</button>{msg&&<p className="status">{msg}</p>}<small>Protected workspace · encrypted provider credentials</small></form></main>}