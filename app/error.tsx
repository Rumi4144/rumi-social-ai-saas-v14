"use client";export default function Error({error,reset}:{error:Error&{digest?:string};reset:()=>void}){return <main className="fatal"><div><span className="eyebrow">SOMETHING WENT WRONG</span><h1>We couldn't finish that action.</h1><p>{process.env.NODE_ENV==="development"?error.message:"Your work is safe. Try the action again."}</p><button className="button" onClick={reset}>Try again</button></div></main>}