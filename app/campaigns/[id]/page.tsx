import {prisma} from "@/lib/prisma";import {notFound} from "next/navigation";
export default async function Campaign({params}:{params:Promise<{id:string}>}){const {id}=await params;const c=await prisma.campaign.findUnique({where:{id},include:{items:true,brand:true}});if(!c)notFound();return <><div className="eyebrow">CAMPAIGN · {c.status.toUpperCase()}</div><h1>{c.title}</h1><p>{c.brand.name} · Goal: {c.goal}</p><div className="campaignitems">{c.items.map(x=><article className="card" key={x.id}><span className="eyebrow">{x.platform} · {x.type}</span><h3>{x.headline||"Creative"}</h3><p>{x.caption}</p><div className="approval"><button>Approve</button><button>Edit</button><button>Regenerate</button></div></article>)}</div></>}