import {NextResponse} from "next/server";import {prisma} from "@/lib/prisma";import {z} from "zod";const S=z.object({jobId:z.string()});export async function POST(req:Request){const p=S.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"jobId required"},{status:400});const j=await prisma.publishJob.update({where:{id:p.data.jobId},data:{status:"retry",lastError:null}});return NextResponse.json(j)}