import {NextResponse} from "next/server";import {z} from "zod";const Brief=z.object({brandId:z.string().min(1),brief:z.string().min(10),goal:z.string().default("launch"),days:z.number().int().min(1).max(90).default(14)});export async function POST(req:Request){const parsed=Brief.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:parsed.error.flatten()},{status:400});return NextResponse.json({status:"accepted",message:"Campaign job accepted. Connect database, auth and queue worker for production execution.",brief:parsed.data},{status:202})}