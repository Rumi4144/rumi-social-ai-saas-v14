import {NextResponse} from "next/server";import Stripe from "stripe";import {prisma} from "@/lib/prisma";import {tenantContext} from "@/lib/auth/context";export async function POST(){try{const ctx=await tenantContext();const sub=await prisma.subscription.findUnique({where:{organizationId:ctx.organizationId}});if(!sub?.stripeCustomerId)return NextResponse.json({error:"No billing customer"},{status:404});const stripe=new Stripe(process.env.STRIPE_SECRET_KEY!);const s=await stripe.billingPortal.sessions.create({customer:sub.stripeCustomerId,return_url:`${process.env.NEXT_PUBLIC_APP_URL}/billing`});return NextResponse.json({url:s.url})}catch{return NextResponse.json({error:"Unable to open billing portal"},{status:400})}}