import {NextResponse} from "next/server";import {prisma} from "@/lib/prisma";import {tenantContext} from "@/lib/auth/context";export async function GET(req:Request){try{const ctx=await tenantContext(new URL(req.url).searchParams.get("organizationId")||undefined);const [s,ledger]=await Promise.all([prisma.subscription.findUnique({where:{organizationId:ctx.organizationId}}),prisma.creditLedger.findMany({where:{organizationId:ctx.organizationId},orderBy:{createdAt:"desc"},take:50})]);return NextResponse.json({balance:s?.credits||0,ledger})}catch{return NextResponse.json({error:"Unauthorized"},{status:401})}}