import {NextResponse} from "next/server";import {prisma} from "@/lib/prisma";import {tenantContext} from "@/lib/auth/context";import {planFor} from "@/lib/billing/plans";export async function GET(req:Request){try{const ctx=await tenantContext(new URL(req.url).searchParams.get("organizationId")||undefined);const s=await prisma.subscription.findUnique({where:{organizationId:ctx.organizationId}});return NextResponse.json({plan:s?.plan||"starter",status:s?.status||"none",credits:s?.credits||0,entitlements:planFor(s?.plan)})}catch{return NextResponse.json({error:"Unauthorized"},{status:401})}}