import {NextResponse} from "next/server";import {prisma} from "@/lib/prisma";export async function GET(req:Request){const u=new URL(req.url);const organizationId=u.searchParams.get("organizationId");if(!organizationId)return NextResponse.json({error:"organizationId required"},{status:400});const jobs=await prisma.job.findMany({where:{organizationId},orderBy:{createdAt:"desc"},take:25});return NextResponse.json({jobs})}