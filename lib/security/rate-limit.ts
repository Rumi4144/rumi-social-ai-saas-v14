import {prisma} from "@/lib/prisma";export async function rateLimit(key:string,limit=60,windowMs=60000){const now=new Date(),floor=new Date(Math.floor(now.getTime()/windowMs)*windowMs);const id=`${key}:${floor.toISOString()}`;try{const b=await prisma.apiRateBucket.upsert({where:{key:id},update:{count:{increment:1}},create:{key:id,windowStart:floor,count:1}});return {ok:b.count<=limit,remaining:Math.max(0,limit-b.count)}}catch{return {ok:true,remaining:limit}}}